---
import { X } from '@lucide/astro';
import type { StarryBioConfig } from '../config/schema';

interface Props {
  status?: StarryBioConfig['status'];
}

const { status } = Astro.props;
---

{
  status?.enabled && (
    <dialog id="status-modal" aria-labelledby="status-modal-title">
      <div id="status-modal-content">
        <button id="status-modal-close" type="button" aria-label="Close schedule">
          <X size={24} aria-hidden="true" />
        </button>
        <h2 id="status-modal-title" class="status-modal-title mb-6 text-center text-2xl font-bold">
          Schedule
        </h2>
        <div class="space-y-6">
          <section>
            <h3 class="status-modal-heading mb-3 text-lg font-semibold">Weekdays</h3>
            <ul id="weekday-schedule-list" class="schedule-list rounded-lg" />
          </section>
          <section>
            <h3 class="status-modal-heading mb-3 text-lg font-semibold">Weekends</h3>
            <ul id="weekend-schedule-list" class="schedule-list rounded-lg" />
          </section>
        </div>
      </div>
    </dialog>
  )
}
